- name: Upload workaround for dying VMs
  hosts: "{{ compute_group_name }}"
  tags:
    - fix_virt_type
  roles:
    - fixer

- name: Fix SSH between compute nodes
  hosts: "{{ compute_group_name }}"
  tags:
    - fix_vms_migration
  roles:
    - nova_migration_fixer

- name: "Set force_config_drive to False in compute nodes"
  hosts: "{{ compute_group_name }}"
  tags:
    - fix_config_drive
  roles:
    - nova_config_drive